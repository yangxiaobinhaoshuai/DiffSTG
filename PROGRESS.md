# PROGRESS

只记大步骤。细碎环节、命令、踩过的坑在 [TODO.md](TODO.md)；代码改动的理由和实验记录在
[reproduction_note.md](reproduction_note.md)。

最后更新：2026-09-01

## 训练进度

| 数据集 | 数据 | 3-seed 训练 | `ddpm`-200 MAE | 耗时 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **PEMS08** | ✅ | ✅ 3/3 | **17.92 ± 0.11** | 22.9 h *(实测)* | **完成** |
| **PEMS04** | ✅ 已转换 | ✅ 3/3 | **22.74 ± 0.18** | 46.8 h *(实测)* | **完成** |
| **PEMS03** | ✅ 已转换 | ⬜ 0/3 | — | ~61 h | 待跑 |

AIR-BJ / AIR-GZ（原文的另两个数据集）**不做** —— 是 PM2.5，不是交通预测。

## 结果

**PEMS08 —— 有原文对照，是整条 pipeline 的协议锚点**

| | MAE | RMSE | CRPS |
| --- | --- | --- | --- |
| **本复现 `ddpm`-200（原文协议）** | **17.92 ± 0.11** | **27.34 ± 0.16** | **0.0611 ± 0.0004** |
| 原文 DiffSTG | 17.68 | 27.13 | ~0.06 |
| SpecSTG 复现的 DiffSTG | 18.99 | 28.26 | 0.0692 |
| （对照）发布代码默认 `ddim_multi`-40 | 21.32 ± 0.31 | 31.04 ± 0.29 | 0.0740 ± 0.0012 |

复现成功，比原文高 0.24 MAE（+1.4%）。PEMS03/04 原文没有、没有已发表数字可对照，
它们的可信度全靠这一行。

**PEMS04 —— 原文没有，无对照**

| | MAE | RMSE | MAPE | CRPS |
| --- | --- | --- | --- | --- |
| **本复现 `ddpm`-200** | **22.74 ± 0.18** | **35.12 ± 0.16** | 15.89 ± 0.24 | **0.0816 ± 0.0006** |

三个 seed 落在 [22.54, 22.89]，全部正常早停，无失败 seed。

**RNG 对照（PEMS08 单 seed 2022）** —— 原版 `evals()` 每 epoch 重置全局 RNG 且不恢复，
于是每个 epoch 重放同一批次顺序、同一组扩散步 t 和同一组噪声 ε：

| | MAE | RMSE | CRPS | best_epoch | 耗时 |
| --- | --- | --- | --- | --- | --- |
| 原版（冻结） | 17.90 | 27.31 | 0.0611 | 173 | 8:59 |
| `--rng_restore 1` | **17.57** | **26.81** | **0.0599** | **78** | **5:35** |

五个指标全好，且**收敛快一倍多**（修正版更便宜，不是更贵）。n=1，约 2.1σ，只定量级。

## 已完成

1. **打通训练/评测流程** —— 修了 4 个原仓库 bug（`torch.load` 的 `weights_only` 静默回退到
   最后一个 epoch、`torch.quantile` 在 test 阶段溢出崩溃、训练循环never调用 `model.train()`、
   test batch 显存是验证的 8 倍）；验证改为固定 512 窗口子集，单 epoch 12.2 min → 2.8 min，
   否则三个 seed 一个都跑不到 test 阶段。
2. **PEMS08 三 seed 跑通** —— 22.9 h。best checkpoint 纳入 git：训练不是逐位可复现的
   （`cudnn.benchmark` 没关），丢了只能按 ~9 h/seed 重训。
3. **定位并修正 test 采样协议** —— 发布代码写死的 `ddim_multi`-40 不是原文协议，它从
   t=0.8N 起步而那里还留着 15.3% 干净信号，值 **3.40 MAE**。换 `ddpm`-200 后 21.32 → 17.92。
4. **补齐 PEMS08 的 mean±std** —— 三 seed 全量 test 集、`n_samples=8`。
5. **接入 PEMS03 / PEMS04** —— 转换脚本（含全部约定校验）+ `default_config` 分支，
   两个数据集端到端冒烟均通过。
6. **PEMS04 三 seed 跑通** —— 46.8 h（估 44 h）。`train.py` 已改为直接跑 `ddpm`-200，
   跑完就是可报告的数字，不再需要从 checkpoint 补测。
7. **定位并量化训练期 RNG 冻结**（原版自带）—— 单 seed 实测值 **0.33 MAE**，修正后
   17.57 已低于原文的 17.68，且收敛快一倍多。`--rng_restore` 默认关，已产出结果不受影响。

## 待做

1. **定 baseline 用哪个 RNG 规则**（挡住后面所有事）—— 用原版就 0 成本但要在 limitation
   里写明那 0.33；用修正版则 PEMS08/PEMS04 六次运行都要重跑。建议先补 PEMS08 的
   seed 2023/2024（约 11 h）把 n=1 变成 n=3 再定。
2. **跑 PEMS03 三 seed** —— 等第 1 条定了再跑，否则可能白跑一遍。
3. **汇总三数据集结果表**，写进论文。
4. **收尾** —— 清理派生数据；开源前在 README 补数据获取说明。

## 已知限制（写论文时要声明）

- **超参沿用原作者在 PEMS08/AIR 上搜出的值**（`N=200`、`beta_end=0.1`、`d_h=32`），
  未做数据集特定调优，可能低估 DiffSTG。**绝不可**出现"自己的方法调参、baseline 用默认值"
  这种不对称 —— 这是答辩时最容易被打的点。
- PEMS03/04 原文没有，**这部分不是"复现"，是把 baseline 扩展到自己的评测集**。
- 三个数据集同为 5 分钟粒度交通流量，证明的是**跨路网稳健性，不是跨领域泛化性**。
- 正确协议下的种子 sd：PEMS08 **0.11 MAE**、PEMS04 **0.18 MAE**（n=3，自由度 2，估计很糙）。
  自己方法的提升若在 0.5 MAE 以内，3 个 seed 可能不够。
- **训练期 RNG 被验证冻结**（原版自带）：每个 epoch 重放同一批次顺序、同一组 `(t, ε)`，
  每个训练窗口整轮只配到一个固定噪声。原文数字和本复现的 PEMS08/PEMS04 都在这个状态下产出，
  实测**值 0.33 MAE**（单 seed），方向上确实在削弱 DiffSTG。**要改必须 baseline 和自己的
  方法一起改**，只修一边就是最经典的那种不对称。
